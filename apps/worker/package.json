{
  "name": "@flowchat/worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/main.ts",
    "lint": "eslint src --max-warnings=0",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@flowchat/config": "workspace:*",
    "@flowchat/contracts": "workspace:*",
    "@flowchat/database": "workspace:*",
    "@flowchat/meta": "workspace:*",
    "@flowchat/security": "workspace:*",
    "bullmq": "^5.58.5",
    "ioredis": "^5.7.0"
  },
  "devDependencies": {
    "@types/node": "^22.18.0",
    "tsx": "^4.20.5"
  }
}
